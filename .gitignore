node_modules/
npm-debug.log*
*.log
.env
.env.*
.DS_Store
*.tmp

# build output
dist/
dist-server/
node_modules/.tmp/

# local sqlite data
data/
*.sqlite
*.sqlite-*

# per-agent local worktrees (scripts/agent-env.mjs) — never commit
.agent-envs/
# ...and the ones the harness makes. These are real git repos, so an untracked
# `git add -A` stages them as gitlinks — a commit that looks fine in --stat and
# is unclonable.
.claude/worktrees/

# playwright mcp scratch output
.playwright-mcp/

# flow MCP server local secrets (staging JWT secret / token)
mcp/*.env
mcp/.secrets.json

.superpowers/
.playwright-cli/
